# Shared packages

- `contracts` — framework-neutral Zod schemas and inferred TypeScript types shared by API consumers and providers.

Future packages such as shared lint configuration, TypeScript presets or UI primitives should be added only after at least two applications need the same stable abstraction.
